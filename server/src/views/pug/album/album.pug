doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="/script/css/album/country_album.css")
        script(src="https://kit.fontawesome.com/809697769a.js" crossorigin="anonymous")
        link(href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet")
        title Document
    body
        header
            div.upload
                span 업로드
                i(class="fas fa-upload")
        main 
            div.imageContainer
                each i in file
                    div.image
                        input.title(type="hidden" value=`${i.title}`)
                        img(src=`/file/upload/image/${i.imageFile[0]}`)
                else 
                    div 게시물이 없습니다.

            div.borderView
                div.imageShow
                    div.selectViewImage
                        img.viewImage(src=`/file/landmark/1.jpg`)
                    div.button
                        div.left
                        div.right
                div.contents
                    div
                    div 
                    div
                
        script(src="/script/js/album/country_album.js")